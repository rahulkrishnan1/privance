// Shared by Dialog and Sheet so the scrim stays in sync.
export const overlayClassName =
  "fixed inset-0 z-50 bg-[rgba(4,6,5,0.72)] backdrop-blur-[7px] transition-opacity duration-200 ease-out data-[starting-style]:opacity-0 data-[ending-style]:opacity-0 data-[ending-style]:duration-150";
