DATABASE_URL=postgres://privance:privance@localhost:5432/privance
PORT=3000
NODE_ENV=development

# Required: base64-encoded random secret >= 32 bytes.
# Used for HMAC-keyed username-enumeration prevention and IP hashing.
# Generate with: openssl rand -base64 48
ENUMERATION_SECRET=

PRIVANCE_SECRETS_DIR=
SIGNUP_ALLOWLIST=
